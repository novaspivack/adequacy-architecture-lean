/-
  NEMS instance discharge — **TODO:** `require nems-lean` (sibling path) and map `diagonal_barrier_rt` / remainder facts (SPEC_001).
-/

import AdequacyArchitecture.Core.Basic

universe u

namespace AdequacyArchitecture.Instances

def NEMSInstancePlaceholder ( _α : Type u) : Prop := True

end AdequacyArchitecture.Instances
